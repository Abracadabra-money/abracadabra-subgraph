{
  "name": "blocks",
  "license": "MIT",
  "repository": {
    "url": "Abracadabra-money/abracadabra-subgraph",
    "directory": "subgraphs/blocks"
  },
  "files": [
    "generated"
  ],
  "scripts": {
    "generate": "pnpm run prepare:yaml && pnpm run codegen",
    "prepare:yaml": "mustache ../../config/$NETWORK.js subgraph.template.yaml > subgraph.yaml",
    "build": "graph build",
    "codegen": "graph codegen",
    "test": "graph test -r"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.62.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0",
    "assemblyscript": "^0.19.20",
    "abi": "workspace:*"
  }
}
